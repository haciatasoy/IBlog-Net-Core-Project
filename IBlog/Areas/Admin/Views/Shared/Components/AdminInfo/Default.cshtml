<a href="#" class="dropdown-toggle active" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    @if (ViewBag.photo != null)
    {
        <img class="user-avatar rounded-circle" src="@ViewBag.photo" alt="User Avatar">
    }
    else
    {
        <img class="user-avatar rounded-circle" src="/UserPictures/@ViewBag.photomain" alt="User Avatar">
    }
</a>